export type ActiveParamsType = {
  categories: string[],
  page?: number,
}

export type CommentParamsType = {
  offset: number,
  article: string,
}

export type UserActionsParamsType = {
  articleId: string,
}

